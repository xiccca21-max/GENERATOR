---
description: T-Bank SBP Proton PASS recipe (seq_1785718550_02) + full charset law
alwaysApply: true
---

# T-Bank SBP PASS recipe (K-TBANK-SBP-PASS-001)

Reference PASS-shaped receipt: `output/night_dual/tbank_sbp/seq_1785718550_02.pdf`
(Павел Соколов → Роман Соколов, 70749, cs_dec=4405, F1 twin glyf=12530 cmap=67, F2 cmap≤9, ~60KB).

## Ship recipe

1. **F1 = corpus SHA twin** when a twin covers the face — FontFile2 byte-identical (no hydrate / orphan blank / lean / CSA mutate).
2. Pick twin from **all corpus PDFs**, not one donor shell charset.
3. **F2 ToUnicode / bfrange &lt; 10** so V3 family stays clear when F1.glyf≤12783.
4. **CS decoded** lands on corpus exact length **naturally** (equal face rewrite) — never `)Tj`/ET space pads (→ CONTENT_SKELETON / ET_WHITESPACE).
5. Merge painted ToUnicode so labels stay «Служба поддержки» / «Телефон» (no PUA/mojibake).
6. Keywords third token by face date (`991` / `DOCS-2035`).
7. Size band ~59–61 KB; value column RIGHT `x1=250`.

## Full charset law (immutable)

- Bot / war / generators must accept **all Cyrillic letters + all digits**.
- Coverage = **union of multi-PDF glyph library / all corpus twins**, never «letters present on one donor».
- If no single twin covers `need_r`: leave twin path, hydrate from multi-corpus raw glyph cache onto a non-twin shell (do not soft-remap user letters as the primary fix).
- Soft-cover lookalikes = last-resort bot anti-GEN_NONE only, not the charset policy.
- Forbidden: twin-safe alphabet quarantine; refusing rare letters because the template donor lacks them.
